# Add -a short flag for --amend in commit command

- ID: d7337743
- Created: 2026-06-15T15:05:22.877Z
- Target: spec/technical-spec.md
- Status: pending incorporation

## Diff

Add `-a` as a short flag for `--amend` in the `sc commit` command.

Users can now use `sc commit -a "message"` as an alias for `sc commit --amend "message"`.
